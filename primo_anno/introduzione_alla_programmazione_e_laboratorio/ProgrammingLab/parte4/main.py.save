class CSVFile():
    def __init__(self, name):
        self.name = name
    def get_data(self):
        with open(self.name, 'r') as f:
            next(f)
            return [line.strip().split(',') for line in f]

ciao sono io il coglione che ti ha appena craccato il computer, la prima mossa che fai ti distrugggo hai capto 
